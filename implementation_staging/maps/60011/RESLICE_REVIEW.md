# 翠溪村横版重新切片（2026-09-15）

用户确认：采用所附原图完整横向布局，1200×600、18列×7行、126块。
本次使用确定性离线裁切，没有 AI 重绘。原图1774×887，等比例缩放，不裁去左右场景。
旧竖版原图保存为 source_scene_portrait_backup.png。

## APK Verification Gate

- 客户端可观察影响：地图画面、碰撞、出生点和返回点。
- 资源：地图60011，图片60011000–60011125；127×127逻辑地图，20×10等距网格。
- 消息路径：沿用1110、1010/12、1010/13、1407/11+12、1010/14、1010/105，实体沿用1126；图片请求沿用1502/1501。没有新增Action、Property或TLV字段。
- APK evidence: B。依据 docs/protocol/map-ref-streaming.md 锁定的 pmsj.work.b.m.z → m.C() 资源解析路径，以及 tools/build_map_60011_apk.py 中已有 images.o 的 >IBH 索引、客户端PNG编码和现有126片资源基线。
- 场景原点：(-600,310)。像素位置保持现有公式 x=10*(tile_x-tile_y)+offset_x、y=5*(tile_x+tile_y+2)+offset_y。
- 本地布局值：出生(76,48)，返回阵(78,50)。碰撞根据图片道路手工描绘并内缩7像素；这些是本地内容，不冒充原服数据。水面和崖壁阻挡，两岸不新增桥梁。远端左岸和右上区域未与出生区建立通路。
- 旧存档：原有90×90布局特殊坐标迁至注册表当前出生点；其他阻挡/越界坐标沿用现有安全迁移。没有直接修改 roles.json。
- Tests: 统一 unittest 198项通过；导出图片后专项7项通过。覆盖无缝拼接、与原图缩放像素一致、126片坐标回算、出生点到返回点及右侧道路连通性、资源返回与PNG编码。
- 构建：以 piaomiao_local_60011_resliced.apk 为基准，只替换60011资源，712个其他APK成员逐字节一致；其他图片索引记录不变。126个APK图片尺寸及服务端镜像一致，map.ref=2272字节。
- APK签名：v1/v2/v3验证通过，使用现有localtest签名。
- 输出：build_artifacts/apk/piaomiao_local_60011_landscape.apk。
- SHA256: a094052e553211274aa94d2057ec02e8a4f32bfe53575cca3047186b91f68534。
- Real-device: pending real-device verification。

## 真机验收

启动/重启本地服务，覆盖安装横版APK，再从长安翠溪村传送阵进入。
预期出生在房屋南侧右岸空地；画面为完整横版布局，移动时切片没有缺块；水面、房屋和崖壁不可穿越，右侧道路能够行走，返回长安传送阵可用。
本次未检测到6805监听服务，没有启动或重启服务；上线后仍需用户真机确认。

## 22:37 运行服务不一致排查

截图角色处于旧坐标(59,42)，旧PID22652的日志也下发该坐标，而磁盘配置出生点为(76,48)。确认旧服务持有旧注册表，新资源已更新。
本次没有改协议或APK；APK evidence沿用B级资源/进入链，修复为重启加载已测试配置。
统一测试201项通过，旧(59,42)迁移到(76,48)检查通过。
通过仓库restart_server.ps1重启；新PID4472监听6805，日志server.20260915-223739.stderr.log确认新地图就绪。
22:37:55日志确认角色10085自动迁移59,42→76,48；22:37:59进入坐标76,48；22:38:01收到1005并记录移动到72,50，之后继续收到移动帧。
端到端test_client.py --exercise-role-crud在装备流程等待1008超时，不能报告端到端全部通过；该问题没有影响日志已确认的本次地图移动。
Real-device: 服务端已收到手机地图移动帧，用户视觉与碰撞验收仍待确认。

## 23:16 新图替换（800×812）

用户补充确认：仍为翠溪村60011，采用附件77cdd899-f4fc-4011-b94c-e77855434f1c.png重新剪切布局。
原图1245×1263，缩放800×812，保留完整比例（高度取整误差小于1像素）；9列×14行126块，图片ID60011000–60011125。
旧横版原图备份为source_scene_landscape_backup.png。使用现有确定性离线切片工具，未重绘素材。
APK evidence: B，沿用前述map.ref解析、PNG索引/编码与进入地图链。Protocol impact: 仅本地地图布局、碰撞、出生/返回位置；MessageID、Action、Property和字段类型/顺序不变。
场景原点(-400,220)，127×127逻辑地图、20×10网格不变；出生(69,49)，返回阵(71,51)，下方道路连通点(86,78)。碰撞按新图描绘并内缩7像素，水面、房屋、崖壁、树与石灯阻挡；左岸和右上道路为独立区域，没有新增桥梁连接。
Tests: 专项7项通过，统一201项通过；覆盖126片无缝拼接、原图缩放一致、坐标回算、出生与返回/下方道路连通、旧位置安全迁移及资源请求。
APK: piaomiao_local_60011_square.apk，v1/v2/v3签名校验通过；签名证书沿用95968928b2ad02fc00657d4f74d4c66bde6094496eda2c03f301e4e5dd514da2。
新旧APK712个其他成员逐字节相同，其他图片索引不变；126个图片尺寸及服务端镜像一致；map.ref2272字节。
SHA256: 218c589d5f247028d1614ac2a85015babf13667d9bff0216e085a33c3eee24a2。
通过仓库restart_server.ps1重启服务；PID32516监听6805，日志server.20260915-231648.stderr.log确认新60011就绪。
TCP验证：test_client.py --host 127.0.0.1 --port 6805 --exercise-logout-only通过。未知新测试账号被原登录策略拒绝，随后使用现有默认测试账号验证通过；没有改认证行为。未再次运行此前在装备步骤超时的完整CRUD链。
Real-device: pending real-device verification。覆盖安装square版APK后重新登录，预期出生在房屋前空地(69,49)，能沿右侧道路移动及返回长安；真机视觉、资源缓存和碰撞仍需用户确认。

## 23:34 石滩两岸通路

用户要求中央石滩可以到达河对岸。本次在碰撞蒙版加入沿中央踏脚石的34像素宽局部浅滩路径，经现有7像素内缩采样后开放41个网格。石头间小间隙按用户要求作为本地浅滩连接，不宣称原服规则；通路外水面继续阻挡。
APK evidence: B，沿用已确认map.o collision编码与1010/12地图下发链；未新增消息/Action/Property或修改字段布局。图片、图块、map.ref、出生与返回点不变。
Tests: 先新增出生(69,49)经石滩(49,61)到左岸道路(41,63)的连通断言并观察失败；实现后专项5项和统一201项通过。河面(58,63)仍阻挡。对比旧APK地图，41个阻挡网格变为开放，没有封闭原有地面；tiles和map.ref逐值/字节相同。
服务已用仓库启动器重启，PID23628监听6805；日志server.20260915-233437.stderr.log确认60011就绪。test_client.py --exercise-logout-only通过。
只更新服务端地图碰撞，无需重装APK。Real-device: pending real-device verification；重新登录后从房屋前向左，经河中中央石滩走至左岸，检查行走、石滩边缘与通路外水面阻挡。

## 矩形切片错误遮挡人物修复

APK evidence: B，直接静态证据 build_artifacts/build/dex-smali/pmsj/work/b/o.smali:80-101将复合图块首字节读入d；o.smali:624-642的a()Z检查d&4；b/m.smali:1258-1272对该分支直接调用o绘入背景，1288-1417非背景分支构造b/l并加入排序队列。b/m.smali:1691后的对象排序使用网格位置/占地，因此整幅场景的矩形切片以flags2绘制会遮挡人物。
修改：126块平面切片flags2→6，开启bit4背景绘制；保留bit2与其余字段。其他未知位没有发明语义。58原图flags统计4:45、2:43、6:26、1:1；60011现在为6:126。
Protocol impact: 仅map.ref flags；没有新增MessageID/Action/Property或改字段类型/顺序。PNG、碰撞、石滩通路、出生点和资源ID不变。平面素材全部画在人物后方，暂不提供独立建筑/树木的真实前后遮挡。
Tests: 背景位回归测试先失败后通过，统一202项通过；服务重启加载更新；test_client.py --exercise-logout-only通过。
视角：b/m.smali:138-163确认i=20、j=10、k=i/2、l=j/2，60011已使用同58的20×10投影。矩形素材与58菱形构图的差异属于美术布局，需要用户选择保留现图调整布局或重绘。
Real-device: pending real-device verification，重新登录验证道路和石滩人物不再被矩形块覆盖。无需重装APK。

## 2026-09-16 景观缓冲边界新图

用户要求采用34edcb02-c9d1-40ba-b4d2-b53bae92a73f.png作为翠溪村，外围一圈不可达以避免边缘黑边。
原图1536×1024，完整等比例缩放1050×700，18列×7行126片，原点(-525,275)，127×127逻辑网格与20×10投影不变；图片ID60011000–60011125，背景flags6不变。
本地布局：四边至少180像素不可走景观，允许区域像素180≤x<870、180≤y<520。相机以角色居中且半视窗不超过180像素时，地图素材可以覆盖视窗；更大视窗、设备缩放或相机偏移仍需真机验收，不能宣称任意显示尺寸绝无黑边。
按新图描绘道路、建筑、崖壁和石灯碰撞；中央浅石滩路径保留两岸连通。出生(73,54)，返回阵(74,55)，道路点(83,63)，左岸道路(45,67)，石滩(48,62)。石间小间隙为用户授权本地浅滩，不冒充原服数据。旧图保存source_scene_square_backup.png；存档由现有阻挡/越界迁移机制保护，没有直接改用户角色。
APK evidence: B。依据前述APK b/m与b/o的map.ref解析、背景绘制、collision和1010/1407进入链；本次仅资源与本地布局变化，没有新MessageID/Action/Property或字段类型/顺序变更。
Tests: 先更新新图与边缘距离断言并观察失败，再生成布局；专项7项和统一203项通过。检查全部可行走网格距四边至少180像素、像素无缝拼接、图片一致、背景标记、出生/返回/道路/石滩/左岸连通、水面阻挡。
APK v1/v2/v3验证通过，原localtest签名；712个其他成员和其他图片索引保持相同，126片打包PNG尺寸与服务端镜像一致。
输出build_artifacts/apk/piaomiao_local_60011_scenery_border.apk；SHA256 2200e9e377d1697d47a0fb2abd912d6c0e47c3426fcc1b6abd7671eebe7f7b9f。
服务已使用restart_server.ps1重启，6805新PID和日志另见本次工具核对。test_client.py --exercise-logout-only通过。
Real-device: pending real-device verification。覆盖安装景观边界版APK后重新登录，沿中央道路和石滩检查两岸通行、外围不可达、人物不被切片覆盖，以及最靠近各边位置有无黑边。
