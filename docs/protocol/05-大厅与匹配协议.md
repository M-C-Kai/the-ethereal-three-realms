# 大厅与匹配协议

当前仓库没有大厅、匹配、队列、分配房间相关协议定义或处理链。角色列表后直接进入人物初始化和地图流程。该缺失是代码事实，不补写推测协议。

证据：服务端完整消息分发位于 `implementation_staging/server.py:1899-2449`；未出现 match/lobby/queue/room 分支。`implementation_staging/README.md:1-20` 列出的运行链路同样直接从角色进入地图。
