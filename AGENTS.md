# AGENTS.md — 飘渺三界项目智能体开发规范

本文件是整个仓库的最高级智能体开发入口。任何 ChatGPT、Codex、OpenCode、Claude、Gemini、自动化 Agent 或其他智能体在**阅读、设计或修改项目之前必须先读本文件**；进入子目录后还必须继续读取该目录的 `AGENTS.md`。

## 必读顺序

在任何修改之前，必须至少阅读：

1. 本文件 `AGENTS.md`。
2. `docs/development/APK_PROTOCOL_FIRST.md`（APK 协议优先开发规范）。
3. 目标目录及其父目录中的 `AGENTS.md`。
4. 与修改相关的协议证据文档、源码、测试和最近实现。

`implementation_staging/` 内的工作还必须阅读 `implementation_staging/AGENTS.md`。

## 最高原则：APK 是客户端契约基准

本项目的目标是让本地兼容服务适配现有《飘渺三界2》APK，而不是重新设计客户端协议。

因此，**任何修改都必须先做 APK 影响复核**：新增功能、Bug 修复、重构、模块迁移、路由调整、字段类型变更、资源 ID 变更、数据模型变更、测试修改和协议文档修改都不能跳过。

- 涉及客户端协议、UI 行为、资源选择、消息号、Action、Property、字段数量/顺序/类型或战斗/地图/物品等客户端可观察行为时，必须先取得 `docs/development/APK_PROTOCOL_FIRST.md` 定义的至少 **B 级证据**，再实现。
- **C/D 级证据不得实现业务语义**；只能继续逆向、抓日志、增加不改变协议的诊断或补充证据文档。
- 不允许为了“先跑起来”猜测 Action、Property、字段类型、字段位置、资源 ID 或客户端状态机。
- 未知字段保持原生安全默认值（如 0/空值）时，必须明确记录“未知”，不能为其发明含义。
- 纯服务端内部重构也必须完成复核：证明其没有改变已锁定的 APK wire contract、客户端可观察行为或资源映射，并用现有回归测试保护该结论。

## APK Verification Gate

每次修改开始前，智能体必须明确回答以下问题：

1. 这次修改是否会影响客户端可观察行为？
2. 涉及哪些 MessageID / Action / Property / 字段 / 资源 / UI 路径？
3. APK 证据等级是多少（A/B/C/D）？证据来自哪些 smali/JAR/反编译调用链/资源/真机结果？
4. 若是纯内部修改，为什么能证明 APK 契约不变？
5. 哪些自动测试验证字段类型、字段顺序、状态迁移和回归？哪些仍需用户真机验收？

没有完成上述复核，不得进入实现。

## 禁止事项

- 禁止根据消息号名称、旧私服习惯或常见 MMORPG 设计猜协议。
- 禁止把数值相同的 `byte/short/int/long/string/binary` 当作可互换类型。
- 禁止添加 APK 中没有依据的新客户端 Action / Property / 消息语义。
- 禁止把测试写成只验证自己臆造的协议，然后用“测试通过”作为 APK 兼容证据。
- 禁止将“代码能运行”描述成“手机已验证”；真机验收由用户完成时必须明确标记 `pending real-device verification`。
- 禁止访问、扫描、认证、修改或抓取官方/第三方服务器；本项目只针对本地兼容服务、用户提供 APK/JAR/资源及仓库内容。

## 修改流程

1. 读取必读文档和目标模块。
2. 复现/确认当前行为。
3. 执行 APK 协议/行为复核并记录证据等级。
4. 若证据不足：停止业务实现，先逆向或增加诊断。
5. 证据达到实现门槛后，先补失败测试，再写最小实现。
6. 保持协议层、业务层、资料库层和系统装配边界清晰；不要把跨域业务重新塞回 `server.py`。
7. 运行相关专项测试和统一测试；网络流改动再运行 `test_client.py`。
8. 服务端需要重启时，只在测试通过后重启并核对 PID/端口/最新日志。
9. 提交说明必须写明 APK 证据、测试结果和真机状态。

## 数据与兼容性

- 保留 `implementation_staging/data/roles.json` 等用户运行存档；数据结构变化必须向后迁移。
- 静态定义（物品、技能、宠物、NPC、地图、Buff、任务等）优先放资料库/registry，不在 handler/service 中散布模板 ID 特判。
- 服务端只实现 APK 已有能力；本地兼容值必须明确标注为本地值，不冒充原服数据。

详细证据等级、Protocol Review、记录模板和验收要求见：

`docs/development/APK_PROTOCOL_FIRST.md`
